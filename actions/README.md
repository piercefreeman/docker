Collection of github actions used to test and build our docker containers.

For security, we fork these from their original sources unless they are published by an official organization (docker/node/etc).
